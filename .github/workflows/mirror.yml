name: Mirror File

on: push
    
env:
  MIR: wss
  FILENAME: origin.7z
  URL: "http://35.238.248.251/Bliss-v14.0-channel-UNOFFICIAL-gapps-20210327.zip"
  
jobs:
  build:
    runs-on: ubuntu-latest


    steps:
       - name: Checkout
         uses: actions/checkout@master
         
       - name: Setup + download
         run: |
              sudo apt install aria2
              aria2c -x16 $URL
              
       - name: artifact
         uses: actions/upload-artifact@v2
         with:
          name: rum
          path: "*.zip"
